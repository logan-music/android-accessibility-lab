name: cyber_accessibility_agent
description: Educational accessibility automation agent for cyber security lab
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^0.13.6
  shared_preferences: ^2.1.1
  uuid: ^3.0.6
  cupertino_icons: ^1.0.6

  # Added for zip handling and small path utilities
  archive: ^3.3.5
  path: ^1.8.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
